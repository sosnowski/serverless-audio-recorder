export interface Recording {
    recordId: string;
    userId: string;
    title: string;
    audio?: AudioInfo;
    created: string;
    updated: string;
}

export interface AudioInfo {
    url: string;
    length: number;
}